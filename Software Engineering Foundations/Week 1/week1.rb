# * removes the brackets [] from an array, i.e. *[1, 2, 3] will return 1, 2, 3
# ** does the same thing except on hashes, removes the {}
# arr.inject{|arr, el| arr + el} takes an accumulator, and adds el, each element to it
    #this would return the sum of arr

    